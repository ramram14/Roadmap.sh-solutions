<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Blog</title>
    <style>
        body {
            font-family: sans-serif, Helvetica;
            max-width: 700px;
            margin: 0 auto;
            padding: 10px;
            background-color: #f4f4f4;
        }

        input,
        textarea {
            padding: 10px;
            border-radius: 5px;
        }

        .btn-back {
            padding: 10px;
            border-radius: 5px;
            cursor: pointer;
            border: 1px solid black;
            font-size: 1rem;
        }

        .btn-submit {
            background-color: lightblue;
            padding: 10px;
            border-radius: 5px;
            cursor: pointer;
            border: 1px solid black;
            font-size: 1rem;
        }
    </style>
</head>

<body>
    <button onclick="window.location.href='/article/<%= blog.slug %>'" class="btn-back">Back</button>
    <h1>Edit Blog</h1>
    <form action="/edit/<%= blog.slug %>?_method=PUT" method="post"
        style="display: flex; flex-direction: column; gap: 1rem;">
        <label for="title">Title</label>
        <input type="text" name="title" id="title" autocomplete="off" value="<%= blog.title %>">
        <label for="content">Content</label>
        <textarea name="content" id="content" cols="30" rows="10"><%= blog.content %></textarea>
        <button class="btn-submit">Update</button>
    </form>
</body>

</html>